name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.2.0'
      SERVERPOD_PASSWORD_database: 'weBsKhSkNDrV3Wcbfjj-5NclQ3K7FVKY'
      SERVERPOD_PASSWORD_redis: 'ovaEMeBWzAmz2zim1LaCAYMMClMO-TKm'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: resonance_server
        run: docker compose up --build -d

      - name: Install dependencies
        working-directory: resonance_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: resonance_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: resonance_server
        run: serverpod generate

      - name: Run tests
        working-directory: resonance_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: resonance_server
        run: docker compose down -v
