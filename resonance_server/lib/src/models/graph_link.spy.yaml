# GraphLink represents an edge in the force-directed graph visualization
# This is a serializable model (not persisted) that aggregates data for the UI
class: GraphLink
fields:
  ### Source node ID
  source: int
  ### Target node ID
  target: int
  ### Weight of the relationship
  weight: double
  ### Relation type
  relationType: String
